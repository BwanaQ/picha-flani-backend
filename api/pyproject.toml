[tool.poetry]
name = "api"
version = "0.1.0"
description = "api for picha flani"
authors = ["Tom Hunja, Philip Mutua"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
